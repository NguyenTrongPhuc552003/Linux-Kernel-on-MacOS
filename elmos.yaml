# ELMOS Configuration
# Paths are auto-computed from project root (PWD)
# Override specific values as needed

build:
  arch: arm64
  # jobs: auto-detected from CPU count
  # llvm: true (default)

image:
  volumename: kernel-dev
  size: 20G

qemu:
  memory: 2G
  # smp: auto-detected from CPU count

# Profiles for different architectures (optional)
# profiles:
#   riscv:
#     arch: riscv
#     cross_compile: riscv64-unknown-linux-gnu-
#   arm64:
#     arch: arm64
#     cross_compile: llvm-
