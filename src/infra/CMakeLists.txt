# ============================================================================
# src/infra/CMakeLists.txt — Infrastructure layer aggregation
# ============================================================================

add_subdirectory(executor)
add_subdirectory(filesystem)
add_subdirectory(platform)
add_subdirectory(homebrew)
