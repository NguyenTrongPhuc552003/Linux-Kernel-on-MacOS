# ELMOS - Embedded Linux on MacOS
# Taskfile for building and installing the elmos CLI
# https://taskfile.dev

version: '3'

vars:
  BUILD_DIR: build
  BINARY: "{{.BUILD_DIR}}/elmos"
  PREFIX: /usr/local
  VERSION:
    sh: git describe --tags --always --dirty 2>/dev/null || echo "dev"
  COMMIT:
    sh: git rev-parse --short HEAD 2>/dev/null || echo "unknown"
  DATE:
    sh: date -u +"%Y-%m-%dT%H:%M:%SZ"
  LDFLAGS: >-
    -X github.com/NguyenTrongPhuc552003/elmos/core/app/version.Version={{.VERSION}}
    -X github.com/NguyenTrongPhuc552003/elmos/core/app/version.Commit={{.COMMIT}}
    -X github.com/NguyenTrongPhuc552003/elmos/core/app/version.BuildDate={{.DATE}}
  GOBIN:
    sh: go env GOPATH 2>/dev/null || echo "$HOME/go"

tasks:
  # ============================================================================
  # Default & Core Build
  # ============================================================================

  default:
    desc: Build the elmos binary
    cmds:
      - task: build

  build:
    desc: Build the elmos binary
    cmds:
      - echo "Building {{.BINARY}}..."
      - mkdir -p {{.BUILD_DIR}}
      - go build -ldflags "{{.LDFLAGS}}" -o {{.BINARY}} ./cmd/elmos
    sources:
      - "**/*.go"
      - go.mod
      - go.sum
    generates:
      - "{{.BINARY}}"

  clean:
    desc: Clean all build artifacts
    cmds:
      - rm -rf {{.BUILD_DIR}}
      - rm -rf completions
      - rm -f coverage.out coverage.html
      - go clean

  # ============================================================================
  # Development Workflow (dev:*)
  # ============================================================================

  dev:deps:
    desc: Download and tidy dependencies
    cmds:
      - go mod download
      - go mod tidy

  dev:fmt:
    desc: Format Go code
    cmds:
      - go fmt ./...

  dev:lint:
    desc: Run linter (golangci-lint or go vet)
    cmds:
      - |
        if command -v golangci-lint >/dev/null 2>&1; then
          golangci-lint run
        else
          echo "golangci-lint not installed, running go vet..."
          go vet ./...
        fi

  dev:check:
    desc: Run fmt, lint, and test (pre-commit style)
    cmds:
      - task: dev:fmt
      - task: dev:lint

  dev:complexity:install:
    desc: Install complexity tools for CC/MI analysis
    cmds:
      - echo "Installing gocyclo and maintidx..."
      - go install github.com/fzipp/gocyclo/cmd/gocyclo@latest
      - go install github.com/yagipy/maintidx/cmd/maintidx@latest
      - echo "Installed to $(go env GOPATH)/bin/{gocyclo,maintidx}"

  dev:complexity:
    desc: Run cyclomatic complexity analysis (CC>10, MI<30)
    vars:
      CC_THRESHOLD: '{{.CC_THRESHOLD | default "10"}}'
      MI_THRESHOLD: '{{.MI_THRESHOLD | default "30"}}'
    cmds:
      - |
        if [ ! -f "$(go env GOPATH)/bin/gocyclo" ] || [ ! -f "$(go env GOPATH)/bin/maintidx" ]; then
          echo "Error: complexity tools not installed. Run 'task dev:complexity:install' first."
          exit 1
        fi
        echo "Running complexity analysis (CC>{{.CC_THRESHOLD}}, MI<{{.MI_THRESHOLD}})..."
        cyclo_output=$($(go env GOPATH)/bin/gocyclo -over {{.CC_THRESHOLD}} . || true)
        mi_output=$(go vet -vettool=$(go env GOPATH)/bin/maintidx ./... 2>&1 || true)

        if [ -n "$cyclo_output" ]; then
          echo "$cyclo_output"
          exit 1
        fi

        if echo "$mi_output" | grep -q "maintidx:"; then
          echo "$mi_output" | grep "maintidx:" || true
          exit 1
        fi

        echo "✓ No complexity warnings!"

  dev:setup:
    desc: Full dev setup (deps + build + init workspace + tools)
    cmds:
      - task: dev:deps
      - task: dev:complexity:install
      - task: build
      - task: elmos:init

  # ============================================================================
  # Installation (install:*)
  # ============================================================================

  install:
    desc: Install to GOPATH/bin
    deps: [build]
    cmds:
      - echo "Installing to {{.GOBIN}}/bin..."
      - mkdir -p {{.GOBIN}}/bin
      - cp {{.BINARY}} {{.GOBIN}}/bin/

  install:global:
    desc: Install to /usr/local/bin (requires sudo)
    deps: [build]
    cmds:
      - echo "Installing to {{.PREFIX}}/bin..."
      - sudo cp {{.BINARY}} {{.PREFIX}}/bin/

  uninstall:
    desc: Remove from GOPATH/bin
    cmds:
      - rm -f {{.GOBIN}}/bin/elmos

  # ============================================================================
  # Release (release:*)
  # ============================================================================

  release:darwin:
    desc: Build for macOS platforms (arm64 + amd64)
    cmds:
      - mkdir -p {{.BUILD_DIR}}
      - echo "Building for darwin/arm64..."
      - GOOS=darwin GOARCH=arm64 go build -ldflags "{{.LDFLAGS}}" -o {{.BINARY}}-darwin-arm64 .
      - echo "Building for darwin/amd64..."
      - GOOS=darwin GOARCH=amd64 go build -ldflags "{{.LDFLAGS}}" -o {{.BINARY}}-darwin-amd64 .

  release:linux:
    desc: Build release binaries for Linux (amd64 + arm64)
    cmds:
      - mkdir -p {{.BUILD_DIR}}
      - echo "Building for linux/amd64..."
      - GOOS=linux GOARCH=amd64 go build -ldflags "{{.LDFLAGS}}" -o {{.BINARY}}-linux-amd64 ./cmd/elmos
      - echo "Building for linux/arm64..."
      - GOOS=linux GOARCH=arm64 go build -ldflags "{{.LDFLAGS}}" -o {{.BINARY}}-linux-arm64 ./cmd/elmos

  release:windows:
    desc: Build release binary for Windows amd64 (requires WSL2 at runtime)
    cmds:
      - mkdir -p {{.BUILD_DIR}}
      - echo "Building for windows/amd64..."
      - GOOS=windows GOARCH=amd64 go build -ldflags "{{.LDFLAGS}}" -o {{.BINARY}}-windows-amd64.exe ./cmd/elmos

  release:completions:
    desc: Generate shell completions
    deps: [build]
    cmds:
      - mkdir -p completions
      - ./{{.BINARY}} completion bash > completions/elmos.bash
      - ./{{.BINARY}} completion zsh > completions/_elmos
      - ./{{.BINARY}} completion fish > completions/elmos.fish
      - 'echo "Completions generated in completions/"'

  release:all:
    desc: Full release (all platforms + completions)
    cmds:
      - task: release:darwin
      - task: release:linux
      - task: release:windows
      - task: release:completions

  release:homebrew:
    desc: Generate Homebrew formula
    cmds:
      - |
        cat <<EOF
        class Elmos < Formula
          desc "Embedded Linux development on macOS - native kernel builds"
          homepage "https://github.com/NguyenTrongPhuc552003/elmos"
          version "{{.VERSION}}"
          license "MIT"

          depends_on "llvm"
          depends_on "lld"
          depends_on "gnu-sed"
          depends_on "make"
          depends_on "libelf"
          depends_on "qemu"
          depends_on "e2fsprogs"
          depends_on "coreutils"

          def install
            bin.install "elmos"
          end

          test do
            system "\#{bin}/elmos", "version"
          end
        end
        EOF

  # ============================================================================
  # Documentation (docs:*)
  # ============================================================================
  docs:
    desc: Build the documentation site
    deps: [docs:install]
    cmds:
      - python3 -m mkdocs build

  docs:install:
    desc: Install documentation dependencies if missing
    internal: true
    cmds:
      - python3 -m pip install mkdocs-material "mkdocstrings[python]" mkdocs_puml --break-system-packages
    status:
      # Only run if all packages are found in the python environment
      - python3 -c "import material, mkdocstrings, mkdocs_puml" 2>/dev/null

  docs:clean:
    desc: Remove generated documentation build files
    cmds:
      - rm -rf build/site/

  docs:serve:
    desc: Serve the documentation site locally and open in browser
    deps: [docs]
    cmds:
      - python3 -m mkdocs serve --open
